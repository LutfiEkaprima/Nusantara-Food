import 'package:flutter/material.dart';

class DitolakView extends StatelessWidget {
  DitolakView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DITOLAK View'),
    );
  }
}
